fastapi>=0.110
uvicorn>=0.29
sqlalchemy>=2.0
pydantic>=2.6
httpx>=0.27
PyYAML>=6.0
jsonschema>=4.21
typer>=0.12
PyJWT>=2.8
beautifulsoup4>=4.12
lxml>=5.1
feedparser>=6.0
python-dateutil>=2.9
pytest>=8.0
celery>=5.3
redis>=5.0
psycopg2-binary>=2.9; platform_system != "Windows"
# 可选:浏览器渲染(政务站等 JS 动态页 / 整页截图)。装后需 `playwright install chromium`,
# 并在设置页开启「启用浏览器渲染/截图」。不装则自动降级为 httpx,不影响其他功能。
playwright>=1.40
