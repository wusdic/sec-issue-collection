# 通用数据采集平台 · 功能清单(v1.0)

> 标记:**通用**=引擎固定逻辑;**参数**=由画像驱动(括号内为 02 文档编号);**平台常量**=与领域无关的共享常量。
> 状态:已有 / 已接线(vX) / P1。类型除 通用 / 参数 / 平台常量 外,v1.3 起标 **组件**(可插拔,不属主线)。主线目标(找得快/找得全/真实可信/好用可用/分类存本地)与功能的对应见 00 总纲 §1。

## 1. 需求画像层

| 功能 | 类型 | 状态 | 说明 |
|---|---|---|---|
| 画像注册与六要素校验 | 通用 | 已有 | `profiles.register_need` |
| 画像→上下文解析(52 项缺省) | 通用 | **本次新增** | `need_ctx.NeedContext`,按需求缓存 |
| 多画像自动发现与注册 | 通用(H) | 已接线(v1.1) | `config/need_*.yaml` 全部注册;启动/CLI/自动运维循环全部需求 |
| 默认需求收敛 | 通用(H) | 已接线(v1.1) | `settings.default_need_id`,替换 40+ 处字面量 |
| `GET /needs` / `GET /needs/{id}/ui` | 通用 | 新增 ui 接口 | 前端据此渲染页签/列名/详情 |

## 2. 源层

| 功能 | 类型 | 状态 |
|---|---|---|
| 种子源载入(幂等,一源多需求) | 参数(C1) | 已有 |
| 六通道源发现 D1–D6 + 候选评分 + 多通道闸门 | 通用;权重 参数(C4) | 已接线(v1.1)(评分改按需求) |
| 主动找源 G2:配方组合、覆盖空白词、词表进化、-site 排除、切题率、引擎熔断 | 通用;配方/自检词/挖词提示 参数(B3,B7,B8) | 已接线(v1.1) |
| 引擎自检与自动调优、候选池自动入库/清理 | 通用 | 已有 |
| 地域/语言判定 | 参数(C6) | 已接线(v1.1)(从 `need.regions/languages` 推导) |
| 通用大平台/跳转域/页脚域/平台号识别 | 平台常量 | 已有;`skip_hosts_extra` 参数(C7) |
| 根域源栏目自动定位(结构一致性+内容相关度) | 通用;提示词/相关词字段 参数(B5,B6) | 已接线(v1.1) |
| 源体检、冗余度判死、复检自愈、转站内检索 | 通用 | 已有 |
| 试运行源自动定级/淘汰 | 通用;官方域/阈值 参数(C3) | 已接线(v1.1) |
| 发布主体信誉定级 + 转载溯源 | 参数(C5) | 已有 |
| 自动运维(9 任务,时间预算)与首次部署流程 | 通用 | 已有;自检词 参数(B8) 已接线(v1.1) |

## 3. 采集层(全部通用/平台常量,已有)
适配器(RSS/列表/搜索引擎×6/公众号/微博/泄露站)、`render=auto`、反爬退避、翻页早停、时效窗口、
广采薄存存档、跳转还原、链接永久化、水位线、诊断留痕、SQLite 短事务并发。

## 4. 处理层

| 功能 | 类型 | 状态 |
|---|---|---|
| URL / 文档 SimHash 去重 | 通用 | 已有 |
| 记录级指纹去重 | 参数(A7) | 已接线(v1.1) |
| 粗筛(相关度阈值双把关) | 提示词 参数(D1) | 已接线(v1.1) |
| 结构化抽取(Schema+词表注入) | 平台通用规则 + 领域规则 参数(D2) | 已接线(v1.1) |
| 范畴闸门(粗筛漏网兜底) | 参数(D3) | 已接线(v1.1) |
| 断言三态守卫(降级/隔离/待人工) | 参数(D4) | 已接线(v1.1)(`money_guard` 逻辑参数化) |
| 完备度评分 | 参数(A8) | 已接线(v1.1) |
| 草稿事件(前缀/查询列/记录类型/置信度) | 参数(A2,A3,A5,A9) | 已接线(v1.1) |
| 复核状态机 + 双签 + 发布红线 | 通用;等级 参数(C2,D5) | 已有(kpi 追溯闸门本次改读 C2) |
| 人工待定队列(重跑/并入/不相干) | 通用 | 已有 |

## 5. 更新层

| 功能 | 类型 | 状态 |
|---|---|---|
| 回访任务生成(T+N)与到期派发 | 参数(E1) | 已接线(v1.1)(原未读画像) |
| 回访触发条件 | 参数(E2) | 已接线(v1.1) |
| 一键检索包 | 参数(E3) | 已接线(v1.1) |
| 名单监控 watch_target | 通用;kinds 参数(E5) | 已有 |
| 对标批次/漏报归因 | 通用 | 已有(模型),流程 P1 |

## 6. 输出层

| 功能 | 类型 | 状态 |
|---|---|---|
| 线索引擎(映射/评分/窗口/话术) | 参数(F1) | 已接线(v1.1);可整体关闭 |
| 报表内核:交叉表 / 金额汇总 / 状态计数 / 缺字段白区 | 参数(F2) | 已接线(v1.1)(四种通用内核) |
| 追溯闸门(确认断言必须挂允许等级) | 参数(F4) | 已接线(v1.1) |
| 日报(结构化+Markdown+邮件) | 参数(F3) | 已接线(v1.1) |
| KPI 看板 | 通用;文案 参数(G5) | 已接线(v1.1) |
| 动作台账(分类分级/升级/确认) | 通用 | 已有 |
| 告警型/订阅型输出 | 参数(F5) | P1 |

## 7. 界面层

| 功能 | 类型 | 状态 |
|---|---|---|
| 需求选择器(多需求切换) | 通用 | 本次新增 |
| 页签启停与命名 | 参数(G2) | 已接线(v1.1) |
| 记录列表列/筛选/详情面板 | 参数(G3,G4,G6) | 已接线(v1.1)(详情用通用渲染器:kv/三态/对象/标签/文本) |
| 仪表盘卡片 | 参数(G5) | 已接线(v1.1) |
| 数据源页(自动化优先布局) | 通用 | 已有 |
| 演示数据 | 参数(F6) | 已接线(v1.1) |

## 8. 离线/测试
| 功能 | 类型 | 状态 |
|---|---|---|
| Mock 模型 | 参数(D6):Schema 骨架 + 画像 mock 规则 | 本次改造(原写死安全关键词) |
| 平台泛化测试集 | 通用 | **本次新增**:政策库走完引擎 |


## 9. v1.2 新增(场景复查)
| 功能 | 类型 | 状态 | 说明 |
|---|---|---|---|
| 范围限定 scope(地域/行业/主题/主体/文种 + require_mention + 时效窗) | 参数(I) | 已接线(v1.2) | 一处声明,五处消费 |
| 关键词自动生成(scope 派生词组 + 静态词组 + 监控名单 + 模型扩展)与配方组合 | 通用;配方 参数(J) | 已接线(v1.2) | `keywords` 能力;setup 自动生成;页面按钮 |
| 找源配方无文件自动生成 | 通用 | 已接线(v1.2) | `discovery_yaml()` 回退 |
| 轻量抽取模式(角色生成 Schema) | 参数(K) | 已接线(v1.2) | 企业动态/地域文件 |
| 普通数值金额汇总 | 参数(F2) | 已接线(v1.2) | 招标金额 |
| 流水线阶段组合 | 参数(K) | 已接线(v1.2) | `pipeline.STAGES` |
| 能力注册表(API/CLI 独立调用) | 通用 | 已接线(v1.2) | `capabilities.py` |
| 监控名单并入检索词 | 通用 | 已接线(v1.2) | WatchTarget → 词组 |

## 10. v1.3 新增(同类项目借鉴,详见 09)
| 功能 | 类型 | 状态 | 说明 |
|---|---|---|---|
| JSON 列表接口适配器 `json_api` | 通用;配置 参数 | 已接线(v1.3) | 官方站 JSON 列表零代码接入 |
| 抓取编码探测(头/meta/猜测) | 平台常量 | 已接线(v1.3) | 政务站 GBK 页不再乱码 |
| 真实性验证(域可信度/哈希/一致/密级)+ 再核查 | 参数(sources.verification) | 已接线(v1.3) | 阶段 `verify`、能力 `verify` / `verify.recheck` |
| 记录关系(废止/替代/修订/依据)谱系 | 参数(quality.relations) | 已接线(v1.3) | `RecordRelation`、阶段/能力 `relations`、`GET /events/{id}/relations` |
| 飞书群通知 + 多维表格导出 | 参数(outputs.exports)/设置 | 已接线(v1.3) | `notify.deliver_feishu`、`exports.run` |
| 四维质量评分卡 | 参数(outputs.quality_scorecard) | 已接线(v1.3) | `kpi.quality_scorecard`、`GET /kpi/scorecard` |
| 义务条款抽取 / 适用性匹配 / 法规层级样板 / 线索触发补爬 | — | P1 | 见 09 §3 |

## 11. v1.3 组件化(主线之外)
| 功能 | 类型 | 状态 | 说明 |
|---|---|---|---|
| 通知组件(渠道注册表:email / feishu / webhook[钉钉/企微/自建]) | 组件 | 已接线(v1.3) | `notify.CHANNELS`、画像 `outputs.notify.channels`、能力 `notify.send` |
| 本地分类资料库导出 `local_library`(JSON + Markdown + 快照路径,按路径模板分目录) | 组件;参数(outputs.exports) | 已接线(v1.3) | "分类存本地"的落点 |
| 飞书多维表格导出 | 组件 | 已接线(v1.3) | 导出目标之一 |
